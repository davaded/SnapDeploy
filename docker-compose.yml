version: '3.8'

services:
  backend:
    image: ${IMAGE_NAME:-ghcr.io/davaded/sitepilot:latest}
    container_name: sitepilot-backend
    env_file:
      - ./backend/.env
    environment:
      - SITES_DIR=/var/www/sites
      - DB_HOST=${DB_HOST:-host.docker.internal} # 使用宿主机 MySQL，必要时改为宿主机 IP
      - DB_USER=${DB_USER:-root}
      - DB_PASS=${DB_PASS:-root}
      - DB_NAME=${DB_NAME:-sitepilot}
      - PORT=3000
      - ADMIN_TOKEN=${ADMIN_TOKEN:-admin123}
      - AUTH_ENABLED=${AUTH_ENABLED:-true}
      - AUTH_SECRET=${AUTH_SECRET}
      - SITE_AUTH_SECRET=${SITE_AUTH_SECRET}
      - SITE_AUTH_COOKIE_SECURE=${SITE_AUTH_COOKIE_SECURE:-false}
    ports:
      - "3000:3000"
    volumes:
      - ./sites:/var/www/sites
